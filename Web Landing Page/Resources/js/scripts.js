{/* <script>
        var CORRECT_ADMIN_NAME = 'Sang';
        var CORRECT_ADMIN_EMAIL = 'admin@gmail.com';
        var CORRECT_ADMIN_PASS = 'admin1';

        var inputAdmin = document.getElementById('adminName');
        var inputAdminemail = document.getElementById('adminEmail');
        var inputAdminpass = document.getElementById('adminPass');

        var adminLog = document.getElementById('adminLog');

        if(adminLog.attachEvent) {
            adminLog.attachEvent('submit', onAdsubmit);
        } 
        else {
            adminLog.addEventListener('submit', onAdsubmit)
        }

        function onAdsubmit {
            var adminName = inputAdmin;
            var adminEmail = inputAdminemail;
            var adminPassword = inputAdminpass;

            if(adminName == CORRECT_ADMIN_NAME && adminEmail == CORRECT_ADMIN_EMAIL && adminPassword == correct CORRECT_ADMIN_PASS) {
                window.location.href = './admin.html;
            }
            else {
                alert('Failed !')
            }
    </script>  */}